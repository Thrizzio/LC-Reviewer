| Prompt / Context                                                                                            | Copilot Suggestion                                                                                                  | Decision | Reasoning                                                                              |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------- |
| "// Node.js + Express: create JWT auth middleware to verify token from cookies using jsonwebtoken"          | Suggested `const token = req.headers.authorization?.split(' ')[1];` and `jwt.verify(token, process.env.JWT_SECRET)` | Modified | Changed token source to `req.cookies.token` to match our cookie-based auth design      |
| "// React (Vite): filter tasks by status using .filter() where status can be 'all', 'completed', 'pending'" | Suggested `.filter(task => status === 'all' ? true : task.status === status)`                                       | Accepted | Correctly handles 'all' case without extra branching and matches expected UI behavior  |
| "// Mongoose: define User schema with email unique and password hashing using bcryptjs pre-save hook"       | Suggested schema without `unique: true` and no pre-save hook for hashing                                            | Rejected | Missing uniqueness constraint and hashing makes it insecure and breaks auth flow       |
| "// Express controller: implement pagination using skip/limit for GET /songs endpoint"                      | Suggested `.skip(page * limit).limit(limit)` approach                                                               | Modified | Replaced with cursor-based pagination using `_id` to avoid performance issues at scale |
| "// React: render list of posts using map and assign key prop"                                              | Suggested `key={index}` in `.map()`                                                                                 | Rejected | Using index as key causes unstable UI during reordering; switched to `key={post._id}`  |
